---
name: toolkit
description: 前端项目交付工作流，覆盖六个阶段——需求理解 / 方案设计 / 接口联调 / 页面实现 / 提交前 review / 地图维护。用户输入 `/toolkit ...`、提到 PRD/需求分析、前端方案设计、接口文档/联调/mock 生成、页面实现与组件复用、提交前 code review，或维护项目规范地图（添加/删除/调整规范、反模式、评审规则、速查项），或在前端项目子模块内做开发任务时使用。不适用于：纯运维操作、纯 git 操作、与代码无关的咨询。
---

# 前端项目交付 toolkit

## 何时使用 / 不使用

**使用**：用户输入 `/toolkit ...`；或提到 PRD/需求拆解、前端方案、接口联调、mock、页面实现、提交前 review；或当前任务在前端项目子模块内开发。

**不使用**：纯 git 操作、纯 CI/CD 配置、纯运维问题、与代码无关的咨询。

## 项目知识层（关键概念）

本 skill 的工作流规则是**通用**的，但每个项目的具体规范、架构、子应用约定等"项目知识"应放在**该项目根的 `.claude/toolkit/`** 目录下。`<项目根>` = 你启动 CC 时的 cwd。

```
<项目根>/.claude/toolkit/
├── PROJECT-MAP.md              ← 子应用/模块概览索引
├── sub-apps/<name>.md          ← 各子应用深度地图
├── review-standards.md         ← 评审标准（通用 + 项目特定）
└── examples.md                 ← 阶段示例
```

具体加载流程和"找不到时怎么办"见"默认执行顺序"第 3 步。

## 默认执行顺序

任何阶段开始前，按以下顺序：

1. **确认输出形态**：和用户对齐这次只是分析、还是要直接改代码、还是要生成可调试页面。形态不明时**先问**，不要自作主张动代码。
2. **定位子模块**：根据用户描述或路径线索，结合项目 PROJECT-MAP 判断属于哪个子模块。无法判断时列出候选让用户选，不要默认全仓扫描。
3. **读 PROJECT-MAP + 深度地图**：

   先检查 `<项目根>/.claude/toolkit/` 是否存在：

   **存在 →**
   - 读 `<项目根>/.claude/toolkit/PROJECT-MAP.md` 中对应子模块条目（轻量概览）
   - 如该条目指向 `sub-apps/<name>.md` 深度地图，**也读它**——读完后大多数任务无需再扫源码（包含架构、组件规范、API 范式、代码风格、反模式、速查表）
   - 地图与代码冲突时**明确指出差异**，建议人工更新地图，**不要**基于过期地图给方案

   **不存在 → 智能推断项目根**：
   1. **从用户消息推断**：消息中若有项目内路径片段（如 `mai/assistant/`、`packages/foo/`、`apps/bar/`、`src/business/...`），在 cwd / 子目录（最多 2 层）/ 父目录（最多 2 层）/ `additionalDirectories` 中找匹配
   2. **从 cwd 上下文推断**：用 ls / Glob 找含 `.claude/toolkit/` 的目录
   3. **候选可信度判定**（满足任一即可信）：含 `PROJECT-MAP.md`（强信号）/ git 根 + 名字匹配 / `package.json` + 名字匹配
   4. **找到 1 个可信候选** → 建议用户二选一：
      - **彻底切**：退出后 `cd <项目根> && claude`（最干净）
      - **仅本次**：用绝对路径读知识层
   5. **找到多个候选** → 列出让用户选
   6. **完全找不到** → 询问是否 bootstrap 一份，或要求用户给绝对路径
   7. **用户选"仅本次"** → 用绝对路径读知识层；任务结束建议沉淀到 `<项目根>/.claude/toolkit/`
4. **估算 context 预算**：列出本次预计要读的文件清单。超过 8-10 个时先输出"读取计划"让用户确认，再开读。
5. **进入阶段路由**：见下方"阶段路由"。
6. **标记人工确认点**：涉及接口字段、鉴权、错误码、边界状态、业务规则的，必须显式列出"待人工校验"。

## args 解析（用户输入路由）

`/toolkit` 后面的全部内容作为 args 传入，按以下规则路由到阶段：

- 第一个识别到的"阶段关键词"决定走哪个阶段（关键词表见下方"阶段路由"）
- 剩余内容作为该阶段的输入材料（PRD 文本、接口链接、文件路径、自然语言描述等）
- 无关键词命中时：先反问用户进入哪个阶段，列出 6 个选项

⚠️ 注意区分：本节是"args → 阶段"的路由规则；输入材料里的 URL / 链接如何获取实际内容，见独立章节"## 输入读取策略"。

## 阶段路由

`/toolkit` 后面用户传入的 args 决定阶段。识别关键词：

| 阶段 | 触发关键词 | 入口动作 |
|---|---|---|
| 需求理解 | 需求 / PRD / 拆解 / 范围 | 走【需求理解】 |
| 方案设计 | 方案 / 设计 / 拆分 / 状态流转 | 走【方案设计】 |
| 接口联调 | 接口 / 联调 / mock / 类型 / Apifox / Swagger | 走【接口联调】 |
| 页面实现 | 实现 / 写页面 / 改组件 / 调试 | 走【页面实现】 |
| 提交前 review | review / 自查 / 提交前 / 预审 | 走【提交前 review】 |
| 地图维护 | 维护类动作（加 / 改 / 删 / 调整 / 维护 / 写进 / 移除）+ 地图类对象（地图 / 规范 / 反模式 / 速查 / 评审标准 / 示例 等）| 走【地图维护】 |

`/toolkit` 不带 args 时：先问用户进入哪个阶段，并列出 6 个选项。

### 跨阶段自动过渡

阶段路由表只描述"用户主动 `/toolkit` 触发"的入口。但 toolkit 也支持**阶段间自动过渡**：

**阶段一 → 后续阶段**：
- 阶段一末尾输出"下一步路径建议"
- 用户回 `继续` → AI **直接进入推荐路径的下一阶段，不需要 `/toolkit` 重新触发**
- AI 进入新阶段时简短播报"现在进阶段 X" 让用户知道当前位置
- 用户回 `走 X 路径` / `跳到阶段X` → 切换路径

**阶段二/三/四 → 下一阶段**：
- 默认**不自动过渡**——这些阶段的产出（方案 / 接口封装 / 代码改动）用户通常需要消化或继续工作
- 用户主动说 `继续走方案的实现` / `进阶段四` → AI 接续进入

**阶段五**：
- review 完成不自动进任何阶段（review 本身是终点）

**任何阶段**：
- 用户主动说 `回阶段一改下需求` / `跳到阶段三` → 切换，AI 携带当前对话上下文进入新阶段，不要求重新输入

### 阶段一：需求理解

输入：PRD 文本/链接，或用户描述。

**第一步：判断需求类型（必须最先输出）**

二选一：
- **【新功能】** — 该子模块范围内首次出现，需要新增模块/路由/接口
- **【原有功能优化】** — 基于现有功能改造（bug 修复 / 交互调整 / 字段补充 / 性能优化 / 文案调整 / 重构等）

判断方法：
1. 看 PROJECT-MAP（含深度地图）中该子模块的"如何判断需求归属"提示
   - PROJECT-MAP 缺失或对应条目为空时退化：grep 关键词 + 看 `<子模块根>/src/` 一级目录结构；仍无法判断直接询问用户
2. grep 关键词（功能名、按钮文案、接口路径）在 src/ 内是否已有命中
3. **不确定时不要默认归类**：列出 1-3 个疑似匹配的现有功能/文件路径，让用户确认

**第二步：按类型走差异化输出**

无论哪类都先输出：
1. **需求类型**：【新功能】/【原有功能优化】
2. **需求摘要**（一两句话）
3. **所属子模块**

**【新功能】额外输出：**
4. **建议落点**：放哪个目录、注册到哪个 entry、是否需要新路由
5. **待确认问题**
6. **实现风险**（重点：是否和已有功能冲突 / 能否复用现有 entry）
7. **建议复用点**：可复用的组件、接口、store

**【原有功能优化】额外输出：**
4. **现状定位**：列出现有相关文件、组件、接口、store（具体到文件路径），说明当前如何运作
5. **改动点**：每条改动落到具体 文件:行号
6. **待确认问题**
7. **回归风险**：改动可能影响的其他页面 / 其他调用点（grep 反查）
8. **建议复用点**：本次是否能顺手复用其他已有能力

**最后必出（无论新功能或优化都要输出）：**

**下一步路径建议**：基于本次需求的类型和复杂度，推荐后续阶段链路。

| 路径名 | 链路 | 适用 |
|---|---|---|
| **完整路径** | 一 → 二 → 三 → 四 → 五 | 新功能 / 大型重构 / 跨子模块 / 涉及新组件或新接口 |
| **精简路径**（跳过方案设计）| 一 → 三 → 四 → 五 | 仅涉及接口对接，改动点明确，无需重新设计模块结构 |
| **极简路径**（直接实现）| 一 → 四 → 五 | UI 微调 / 文案修改 / bug 修复 / 不涉及接口、路由、新组件 |
| **仅 Review** | 一 → 五 | 用户只想看需求理解，不动代码 |

**默认推荐规则**：
- 【新功能】→ 默认 **完整路径**
- 【原有功能优化】→ 按改动复杂度自动选：
  - 改动 ≤ 3 文件 + 不涉及新接口 / 路由 / 组件 → **极简路径**
  - 改动 ≤ 3 文件 + 涉及接口对接 → **精简路径**
  - 改动 > 3 文件 OR 跨子模块 OR 涉及新组件 → **完整路径**

**输出格式**（一段固定文本结尾，AI 必须按此格式给出）：

> 下一步路径建议：**<推荐路径名>**
> 理由：<一两句话说明为什么这个路径足够>
> 备选：<列出其他可行路径名 + 适用场景>
> 你回复 `继续` 走推荐路径 / `走完整路径` / `跳到阶段X`（如 "跳到阶段三"）。

**用户回复处理**：
- 回 `继续` → AI **不要等用户再打 `/toolkit`**，直接进入推荐路径的下一阶段
- 回 `走 X 路径` / `跳到阶段X` → 切换到指定路径
- 回别的（继续讨论 / 修正理解）→ 留在阶段一，等用户确认后再进入后续

不脱离代码库做泛化建议；找不到对应位置时按项目约束 #5（承认找不到，不编造）。
**找不到的项必须归入响应末尾的"未存档发现"清单**——类型按差异处理章节判断（通常是 `描述误差` 或 `归类待确认`），便于事后存档或回顾。

### 阶段二：方案设计

**前置上下文**（应来自阶段一）：
- 需求类型（【新功能】/【原有功能优化】）
- 需求摘要 + 所属子模块
- 阶段一已输出的"待确认问题 / 风险 / 复用点"——方案要直接消化这些信息，不要让用户重复说

**如果用户跳过阶段一直接 `/toolkit 方案`**：
- 反问："先做需求理解还是直接出方案？跳过阶段一，方案准确度会下降"
- 用户坚持直接出 → 按通用方案模板走（不区分类型差异化），最后建议"考虑回阶段一补全分析"

**如果是【原有功能优化】**，方案输出要追加：
- **改动 diff 概览**：每个文件的改动点（增/删/改）和原因
- **兼容性**：旧调用点、旧数据格式、旧 URL 是否还能工作
- **回归风险与建议测试范围**
- **可逆性**：是否能灰度 / 回滚

输出：
- 页面结构与模块拆分
- 组件复用建议（按"复用查找规则"走，**必须给具体文件路径 + 行号**）
- 路由接入方式（接入哪个 router 配置）
- 状态管理方案（与该子模块现有 store 模式一致）
- 接口接入方案（请求封装层位置）
- mock 策略
- 调试方案
- 风险点和人工确认点

避免引入子模块现有体系外的新模式（新状态库、新样式方案等），除非用户明确要求。

### 阶段三：接口联调

**前置依赖**：`sub-apps/<name>.md` 的"API 调用范式 / 错误码处理"两节。
- 深度地图存在 → 严格按其约定走（请求封装位置、错误码分层、字段命名）
- 深度地图缺失 / 无对应章节 → 按通用前端约定走（复用项目现有 request 工具，HTTP 标准状态码处理），任务结束建议沉淀进 `sub-apps/<name>.md`

支持的输入：
- **首选**：可被 fetch 的 OpenAPI/Swagger JSON、Apifox 分享链
- **次选**：粘贴的接口字段表
- **不支持**：截图（明确告知用户需要文字化）

产出：
1. 前端请求封装（按"复用查找规则"走，**复用该子模块现有的 request 封装，不要新造**）
2. TypeScript / JSDoc 类型定义
3. mock 数据：覆盖正常态、空态、异常态、权限不足、分页边界
4. 可调试页面骨架（如用户要求）

强制提示：mock 仅用于联调前置验证，**不代表线上契约**；接口字段、错误码、鉴权、边界场景**必须人工确认**。

### 阶段四：页面实现与调试

**前置依赖**：`sub-apps/<name>.md` 的"组件规范 / 代码风格 / 反模式 / 速查表"几节。
- 深度地图存在 → 严格按其约定写代码（命名 / import 顺序 / 样式块 / 禁用废弃组件）
- 深度地图缺失 / 无对应章节 → 通过 ls 子模块 + 看几个相邻文件归纳风格；任务结束建议沉淀

执行规则：
- 优先复用现有页面、组件、hooks、工具函数（按"复用查找规则"走）
- 严格遵循该子模块已有的目录结构和命名风格
- 样式规则（按子模块技术栈匹配，**默认不写 inline style**）：
  - **React/Vue 项目**：单独 SCSS / CSS Module 文件，禁止 inline style
  - **Svelte 项目**：`<style lang="scss">` 块，禁止 inline style
  - **其他栈 / 不确定**：遵循该项目 PROJECT-MAP 或深度地图里规定的样式约定
- 不引入新模式（新状态库、新样式方案、新构建配置等）
- 给出 mock 驱动 + 状态验证的调试建议

### 阶段五：提交前 review

按 `<项目根>/.claude/toolkit/review-standards.md` 执行。如果该文件不存在，按通用 review 维度走（逻辑 / 空值边界 / 错误处理 / 命名 / 状态流转 / 复用 / 样式规范 / 风险点），并提示用户考虑沉淀一份项目特定标准。

输出格式：
- **阻塞项**（必须修）
- **建议项**（可选）
- **未发现问题项**（明确说"未发现 X 类问题"，不要凑）

明确边界：AI review 是预检，不替代人工 review、测试和业务验收。

### 阶段六：地图维护

**"地图"的定义**：toolkit skill 在该项目应遵循的**全部项目规范知识库**，不只是速查表。
具体涵盖（按规范类型）：

- **架构与模块结构**：项目骨架、入口、生命周期、子模块边界
- **代码规范**：缩进、引号、命名、导入顺序、注释风格
- **组件 / API / Store 范式**：标准写法、props 风格、错误码分层、订阅约定
- **样式规范**：禁止 inline style、SCSS 组织、变量来源
- **反模式与已知坑**：废弃组件、已注释代码、内存隐患、真实 bug
- **速查表**：找 X 去哪、何时用 Y
- **评审规则**：通用 + 项目特定
- **阶段示例**：约束 AI 输出形态

**维护范围**：`<项目根>/.claude/toolkit/` 下所有 `.md`（`PROJECT-MAP.md`、`sub-apps/*.md`、`review-standards.md`、`examples.md`）。
**SKILL.md 本身不在范围内**——只能由用户手动改（user-scope 在 `~/.claude/skills/toolkit/SKILL.md`，项目覆盖版在 `<项目根>/.claude/skills/toolkit/SKILL.md`）。

**操作类型**：
- **添加**：新规则 / 新反模式 / 新速查项 / 新模块条目 / 新评审标准 / 新示例
- **删除**：移除过期或错误的规则
- **修改**：调整措辞、范围、判断条件
- **重构**：拆分 / 合并 / 跨文件迁移

**执行步骤**：
1. **解析意图**：识别操作类型 + 规范类型（架构 / 代码规范 / 范式 / 样式 / 反模式 / 速查 / 评审 / 示例）+ 目标文件 + 目标位置。**不确定先问**，不要乱猜
2. **读目标文件**：定位章节，提取已有同类条目的格式作为风格参考
3. **校验**：
   - 添加 → grep 目标位置确认无同名条目；有则告知"已存在"
   - 删/改 → grep 确认目标存在；找不到则列出疑似匹配项让用户选
4. **生成"改动提案"**（格式见下），等用户确认
5. **用户回复后**：
   - `确认` → 用 Edit 应用
   - `调整：<新描述>` → 重新生成提案
   - `取消` → 不动
6. **应用完成后**：检查"关联建议"中的同步——若需要，每个相关文件**单独**走一次提案流程

**改动提案格式（必须遵循）**：

```
## 改动提案

- **目标文件**：<相对路径>
- **目标位置**：<章节 / 锚点>
- **规范类型**：架构 / 代码规范 / 范式 / 样式 / 反模式 / 速查 / 评审 / 示例
- **操作类型**：添加 / 删除 / 修改 / 重构
- **理由（推断）**：<一句话>

### 应用前
<原内容片段；新增则写"（新增条目）"；删/改则贴现有片段>

### 应用后
<最终内容片段，风格与该文件已有条目对齐>

### 关联建议（可选）
<可能也要同步的其他位置；不强制；写"无"也可>

---
回复 `确认` 应用 / `调整：<新描述>` 重做 / `取消` 终止
```

**注意事项**：

- 维护"**反模式**"必须用户给出明确依据（出过事故 / 被废弃 / 性能问题），AI 不凭主观判断给某种写法定性为反模式
- 修改"**代码规范**"时若与权威配置（.editorconfig / .prettierrc / svelte.config 等）冲突，必须明确指出冲突，让用户决定以哪个为准
- 维护"**评审规则**"的项目特定分区时，应主动联想是否能从对应深度地图里抽出依据
- 用户描述模糊时（如"改下样式规则"）先反问具体改什么
- 涉及多文件时每个文件**单独**出一份提案，不合并
- 不"扩写"用户意图——多余想法放"关联建议"，让用户决定

## 复用查找规则（横跨阶段二/三/四）

任何"建议复用现有 X"（组件 / 函数 / 接口封装 / Hook / 工具）必须按以下顺序找，**不要凭印象**：

1. **看深度地图速查表**（`sub-apps/<name>.md` 末尾的"找 X 去哪"表）→ 命中条目直接用
2. **ls 子模块对应目录**（组件去 `component/`、hook 去 `hooks/`、dao 去 `dao.js` / `api/`）
3. **grep 关键词**（功能名 / 命名约定 / 类似 props 字段）

找到结果时**必须列具体文件路径 + 行号**让用户确认，不要泛指"参考已有组件"。

三步都找不到 → 输出"无可复用项，建议新建"，**不要为了凑"复用建议"硬指**。

## 差异处理与存档建议（横跨所有阶段）

任何阶段执行中遇到"差异"，AI 必须**显式标注**（不是默默处理）。

### 主动询问是否存档的两类

#### 1. AI 推断被用户纠正

**触发**：用户用纠正语气回复（"不对" / "不是" / "我要的是 X 不是 Y" / "你判错了" 等）

**AI 行为**：先按用户的纠正继续做事，本次任务结束时追加：

```
⚠️ 你刚才纠正了我的判断（我判 X，实际是 Y）。
这次的判别信号是否要沉淀进地图，避免下次再错？
常见可沉淀点：识别口径 / 关键词 / 判别信号 / 命名约定
- 回 `存档` → 走阶段六生成改动提案，把这条规则加到对应的 sub-apps/<name>.md 或 PROJECT-MAP.md
- 回 `不用` → 仅记到本次"未存档发现"清单
```

#### 2. Review 中识别出"似曾相识"的项目特定问题

**触发**：阶段五 review 时，AI 通过语义识别或 grep 验证发现某个问题在该项目代码里**反复出现**（多个文件命中），且 `review-standards.md` 的"项目特定规则"分区里**没有对应规则**

**AI 行为**：在 review 输出末尾追加：

```
⚠️ 本次发现的"<问题描述>"似乎是该项目反复出现的问题（命中：A、B、C）。
要不要补进 review-standards.md 的"项目特定规则"分区，让以后 review 直接列为阻塞项？
- 回 `补` → 走阶段六生成改动提案
- 回 `不补` → 仅记到"未存档发现"清单
```

### 仅标注、不询问的其他差异

以下差异 **必须显式标注**，但**不主动询问存档**（避免输出过于啰嗦）：

- 用户描述的位置/功能找不到 → 标注"找不到 X，是否表述有误？"
- 地图与代码冲突 → 标注"地图说 A，代码是 B，按代码处理"
- 用户描述疑似新功能但地图已存在类似功能 → 列疑似匹配让用户确认（这条已在阶段一"判断方法"中规定）

这些差异**仍要进入响应末尾的"未存档发现"清单**——便于用户回头自行决定要不要存档。

### "未存档发现"清单（响应末尾固定区块）

每次响应末尾，如果有任何未存档的差异，固定列一段：

```
## 未存档发现
- [类型] 位置/章节：内容简述
- [类型] 位置/章节：内容简述
...
（如需存档其中任意一条，调 /toolkit 阶段六）
```

类型枚举：`描述误差` / `地图过时` / `归类待确认` / `判别纠正` / `项目特定坑`

如果本次响应没有任何未存档发现，该清单不必输出。

## 项目约束（硬性）

1. 默认操作范围限定在用户当前任务相关的子模块，不擅自跨模块改动。
2. 不修改子模块的构建配置（vite/webpack/rollup config）和依赖版本，除非用户明确要求。
3. 样式禁止 inline `style="..."`，按子模块的样式组织方式走。
4. 接口字段、错误码、鉴权逻辑必须人工校验，不在 AI 输出里默认正确。
5. 找不到信息时**承认找不到**，不要编造路径、组件名、接口字段。

## 输入读取策略

**口诀**：**读取找 Playwright，调试找 chrome-devtools**。两类工具各司其职，不要混用。

### 用户显式指定 > 自动选择

如果用户在请求里**点名了具体 MCP**（措辞如 "用 playwright 读"、"use playwright-bg to xxx"、"走 chrome-devtools"、"试试 browsermcp"、"换 X 试一下"），AI 必须**优先用用户指定的工具**，跳过下面的自动决策流程。

用户指定的工具失败时：
- 报告**具体失败原因**（连不上 / 工具不存在 / 实际错误）
- **不要自作主张换工具**——询问用户："要不要走默认自动链路 / 改用 <另一个工具> / 让你贴正文"
- 用户决定后再继续

如果用户没显式指定，按下面"自动决策"走。

### 自动决策

不同输入类型走不同路径：

- **本地路径** → 直接 Read
- **公开 URL**（GitHub raw、公网文档、Swagger JSON 等）→ WebFetch
- **登录态文档链接**（feishu.cn / lark.com / notion.so / 内网 wiki / 私有 Apifox / Confluence 等）：

  ⚠️ **不要直接 WebFetch**（99% 拿到登录页）。统一走 Playwright 链路：

  1. **首选：`playwright-bg`**（headless，无窗口、低干扰）
     - 适合"共享 profile 已登录过该站点"的场景——直接读出来
     - 出现"内容空 / 拿到登录页 / 卡 30 秒以上"立即转步骤 2，不要硬等

  2. **首次登录 / `playwright-bg` 失败 → `playwright`**（可见 Chromium）
     - 弹出窗口，让用户**在窗口里**手动登录
     - ⚠️ **严禁**让用户把账号密码 / 2FA 贴到 CC 对话框
     - 登录成功后 cookies 自动写入共享 profile（默认路径：`C:/Users/<user>/.browser-mcp-profile`），之后回到 `playwright-bg` 就能直接读

  3. **Playwright 也不行 / 用户不想再额外登一次 → 提醒用户尝试 Browser MCP**
     - [Browser MCP](https://browsermcp.io) 通过浏览器扩展**接管用户日常 Chrome**，直接复用日常浏览器里已登录的状态——不需要再单独登录一次
     - AI 行为：
       - 环境里检测到 `mcp__browsermcp__*` 工具 → 告知用户"还有一招：可以让 Browser MCP 接管你日常 Chrome 直接读（前提你日常浏览器登录过这个站点；隐私上 AI 会看到你浏览器当前页面）"，用户同意后调用
       - 检测不到 → 告知用户"还可以装 [Browser MCP](https://browsermcp.io) 作为备选——它接管日常 Chrome 不用再登一次，要不要现在装？"
     - 用户拒绝 / Browser MCP 也读失败 → 进步骤 4

  4. **以上都不行** → 让用户三选一：
     - 粘贴正文（最快）
     - 导出 Markdown / PDF 后给本地路径
     - 在浏览器打开后复制全文粘贴

- **需要调试网页**（抓 XHR 接口返回 / 看控制台报错 / 跑 Lighthouse / 性能 trace / 内存快照）→ **`chrome-devtools` MCP**
  - 这是 chrome-devtools 的本职工作，**不要用 Playwright 强行替代**（很别扭，缺接口监听 / Lighthouse / 性能分析能力）
  - 不涉及登录态时直接用
  - **不要拿 chrome-devtools 当读取工具**——它做内容提取不顺手，用错地方双方都难受

- **截图** → **不接受**——明确要求用户文字化（粘贴或 OCR 后再来）

**通过 MCP 自动读取的登录态内容**：必须告知用户"自动读取的内容请人工核对，登录态文档常有动态加载、权限差异、富文本结构丢失等问题"。

### MCP 工具职责速查

| 想做的事 | 用哪个 MCP |
|---|---|
| 读登录态页面（飞书 / Notion / wiki / 私有 Apifox 等） | `playwright-bg` 优先 → 失败回退 `playwright`（首次登录可见窗口）→ 仍失败提醒用户尝试 `browsermcp`（接管日常 Chrome）→ 都不行让用户贴正文 |
| 读公开静态页面 / RSS / API JSON | WebFetch（轻量，不用拉浏览器） |
| 抓 XHR 接口返回 JSON | `chrome-devtools` |
| 看页面控制台报错 | `chrome-devtools` |
| 跑 Lighthouse 评分 | `chrome-devtools` |
| 性能 trace / 卡顿分析 / 内存泄漏 | `chrome-devtools` |
| 操作页面（点按钮、填表、选下拉、上传文件） | `playwright`（要可视）/ `playwright-bg`（无界面批量） |
| 截屏 / 保存 PDF | `playwright`（结果给用户）/ `chrome-devtools`（调试中顺手）皆可 |

## 辅助参考文件

- `<项目根>/.claude/toolkit/PROJECT-MAP.md` — 子模块地图（概览级），进入任一阶段前先读对应条目
- `<项目根>/.claude/toolkit/sub-apps/<name>.md` — 子模块深度地图（架构、组件规范、API 范式、代码风格、反模式、速查表）。**进入任一阶段前对所属子模块的深度地图也必读**——读完后大多数任务无需再扫源码，能显著降低 token 消耗
- `<项目根>/.claude/toolkit/review-standards.md` — 阶段五的 review 标准
- `<项目根>/.claude/toolkit/examples.md` — 各阶段输入输出示例

## 多项目使用与覆盖机制

- 本 skill 在 user-scope（`~/.claude/skills/toolkit/`）下，**任何项目内都可调用**
- 项目特定知识在每个项目的 `<项目根>/.claude/toolkit/` 下，自管理
- 如果某项目需要**完全定制**工作流（不只是知识层差异，连阶段都不同），可在该项目根放 `<项目根>/.claude/skills/toolkit/SKILL.md`，CC 会优先用项目级版本覆盖 user-scope
